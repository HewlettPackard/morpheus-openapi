type: object
required:
  - access
properties:
  allVdiPools:
    type: boolean
    description: Apply to all VDI pools. Alias of `all`.
  all:
    type: boolean
    description: Alias for `allVdiPools`
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevelsForResource.yaml
