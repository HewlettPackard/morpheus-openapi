type: object
required:
  - allClusterTypes
  - access
properties:
  allClusterTypes:
    type: boolean
    description: "Apply to all cluster types"
  all:
    type: boolean
    description: Alias for `allClusterTypes`
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevelsForResource.yaml