type: object
required:
  - allCatalogItemTypes
  - access
properties:
  allCatalogItemTypes:
    type: boolean
    description: "Apply to all catalog item types"
  all:
    type: boolean
    description: Alias for `allCatalogItemTypes`
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevelsForResource.yaml