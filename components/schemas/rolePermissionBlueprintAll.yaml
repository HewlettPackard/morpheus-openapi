type: object
required:
  - access
properties:
  allAppTemplates:
    type: boolean
    description: Apply to all blueprints (appTemplate). Alias of `allBlueprints`.
  allBlueprints:
    type: boolean
    description: Alias for `allAppTemplates`
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevelsForResource.yaml
