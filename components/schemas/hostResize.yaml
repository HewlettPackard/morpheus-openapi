server:
  type: object
  properties:
    plan: 
      type: object
      properties: 
        id: 
          type: integer
          format: int64
          description: The ID of the new plan (optional). See Available Service Plans
servicePlanOptions:
  $ref: servicePlanOptions.yaml
volumes:
  type: array
  description: List of volumes with their new sizes.
  items:
    $ref: instanceCreateVolume.yaml
storageController:
  type: array
  description: |
    Optional array of storage controller configurations. When provided, additional disk controllers will be
    created (or existing ones updated by id) during the resize. Supported on VMware and MVM.
  items:
    $ref: instanceCreateStorageController.yaml
deleteOriginalVolumes:
  type: boolean
  description: Delete the original volumes after resizing. (Amazon only)
  default: false
networkInterfaces:
  type: array
  description: Key for network configurations. Include id to update an existing interface.
  items:
    $ref: instanceCreateNetwork.yaml