type: object
description: Parameters used to capture a new Host Configuration Profile.
required:
  - name
properties:
  name:
    type: string
    description: Unique name for the new profile.
    example: pre-replacement-baseline
  description:
    type:
      - string
      - 'null'
    description: Optional description for the new profile.
    example: Captured before host replacement

