type: object
required:
  - allInstanceTypes
  - access
properties:
  allInstanceTypes:
    type: boolean
    description: "Apply to all instance types"
  all:
    type: boolean
    description: Alias for `allInstanceTypes`
  access:
    type: string
    description: The new access level.
    enum:
      $ref: rolePermissionAccessLevelsForResource.yaml