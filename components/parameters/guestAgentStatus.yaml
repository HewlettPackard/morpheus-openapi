name: guestAgentStatus
in: query
description: Filter by Guest Agent Status. 
schema:
  type: string
  enum:
    - connected
    - disconnected
    - configured
    - unconfigured
    - unknown
    - error
examples:
  connected:
    $ref: '../examples/guestAgentStatus.json#/connected'
  disconnected:
    $ref: '../examples/guestAgentStatus.json#/disconnected'
